#include <iostream>
#include <cmath>

int main() {
    int a,b,c; 
    std::cin >> a >> b >> c;
    std::cout << (-b + sqrt(b*b-4*a*c))/2*a << std::endl; 
    std::cout << (-b - sqrt(b*b-4*a*c))/2*a << std::endl;
    
    return 0;
}
